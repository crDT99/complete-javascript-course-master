'use strict'; //the strict mode must be the first line
// without the strict, the program can ignore things that may cause trouble, like bad variable names or reserved words
//example

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true;
if (hasDriversLicense) console.log("I can Drive");